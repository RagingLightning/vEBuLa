<UserControl x:Class="vEBuLa.Views.ServiceNumberPopupV"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:vEBuLa.Views"
             xmlns:vm="clr-namespace:vEBuLa.ViewModels"
             mc:Ignorable="d" 
             Height="600" Width="800">
    <Canvas Panel.ZIndex="100" Background="Transparent" IsHitTestVisible="False" d:DataContext="{d:DesignInstance vm:ServiceNumberPopupVM}" FocusManager.FocusedElement="{Binding ElementName=txtService}">
        <Border Canvas.Left="108" Canvas.Top="198" BorderThickness="2" BorderBrush="LightGray">
            <DockPanel d:Visibility="Visible" Canvas.Left="110" Canvas.Top="200" Background="LightGray" Width="580" Height="200">
                <TextBlock DockPanel.Dock="Top" Text="Zugnummerneingabe" Background="DarkBlue" Foreground="White" FontSize="16"/>
                <Grid Margin="15,20,15,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Zugnummer: " TextAlignment="Right" VerticalAlignment="Center" Margin="0,0,-15,0"/>
                    <TextBox x:Name="txtService" Text="{Binding ServiceNumber, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Width="100" Height="20" HorizontalAlignment="Left" Margin="15,0,0,0"/>

                    <Border Grid.Row="0" Grid.Column="2" HorizontalAlignment="Right" BorderThickness="1,1,0,0" BorderBrush="Black">
                        <Border BorderThickness="0,0,1,1" BorderBrush="White">
                            <Button Width="25" Height="25" Content="C"/>
                        </Border>
                    </Border>

                    <Grid Grid.Row="1" Grid.Column="2" HorizontalAlignment="Right">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Fahrplan laden" TextAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <Border Grid.Column="1" HorizontalAlignment="Right" BorderThickness="1,1,0,0" BorderBrush="Black">
                            <Border BorderThickness="0,0,1,1" BorderBrush="White">
                                <Button Width="25" Height="25" Content="E"/>
                            </Border>
                        </Border>
                    </Grid>

                    <Grid Grid.Row="2" Grid.Column="2" HorizontalAlignment="Right">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Übersicht" TextAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <Border Grid.Column="1" HorizontalAlignment="Right" BorderThickness="1,1,0,0" BorderBrush="Black">
                            <Border BorderThickness="0,0,1,1" BorderBrush="White">
                                <Button Width="25" Height="25" Content=">"/>
                            </Border>
                        </Border>
                    </Grid>

                    <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" BorderThickness="1,1,0,0" BorderBrush="Black" VerticalAlignment="Center">
                        <Border BorderThickness="0,0,1,1" BorderBrush="White">
                            <TextBlock Text="Zugnummer eingeben, Fahrplan laden mit E, Übersicht mit Pfeil nach rechts, Korrigieren mit C"/>
                        </Border>
                    </Border>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="RagingLightning"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="vEBuLa-Version   0.1.0" TextAlignment="Center"/>
                    <TextBlock Grid.Row="4" Grid.Column="2" Text="Funk" TextAlignment="Center"/>
                </Grid>
            </DockPanel>
        </Border>
    </Canvas>
</UserControl>
