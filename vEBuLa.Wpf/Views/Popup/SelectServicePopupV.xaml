<UserControl x:Class="vEBuLa.Views.SelectServicePopupV"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:vEBuLa.Views"
             xmlns:vm="clr-namespace:vEBuLa.ViewModels"
             mc:Ignorable="d" 
             Height="600" Width="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:EbulaServiceVM}">
        </DataTemplate>
        <Style x:Key="ListContainerStyle" TargetType="ListViewItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                        <Grid>
                            <Rectangle/>
                            <Label Content="{Binding Info}" Padding="0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Selected}" Value="True">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                <Grid>
                                    <Rectangle Fill="DarkBlue"/>
                                    <Label Foreground="White" Content="{Binding Info}" Padding="0"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ListView">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ListContainerStyle}"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Visible"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
    </UserControl.Resources>
    <Grid Panel.ZIndex="100" Background="Transparent" IsHitTestVisible="False" d:DataContext="{d:DesignInstance vm:SelectServicePopupVM}">
        <Border VerticalAlignment="Center" HorizontalAlignment="Center" BorderThickness="2" BorderBrush="LightGray">
            <DockPanel d:Visibility="Visible" Canvas.Left="110" Canvas.Top="200" Background="LightGray" Width="760" Height="400">
                <TextBlock DockPanel.Dock="Top" Text="Fahrplanauswahl" Background="DarkBlue" Foreground="White" FontSize="16" Margin="0,0,0,15"/>
                <DockPanel Margin="0,0,15,15" DockPanel.Dock="Right" LastChildFill="False">
                    <Border DockPanel.Dock="Top" HorizontalAlignment="Right" BorderThickness="1,1,0,0" BorderBrush="Black">
                        <Border BorderThickness="0,0,1,1" BorderBrush="White">
                            <Button Width="25" Height="25" Content="C"/>
                        </Border>
                    </Border>
                    <Border DockPanel.Dock="Bottom" BorderThickness="1,1,0,0" BorderBrush="Black">
                        <Border BorderThickness="0,0,1,1" BorderBrush="White">
                            <Button Width="25" Height="25" Content="E"/>
                        </Border>
                    </Border>
                    <Border DockPanel.Dock="Bottom" BorderThickness="1,1,0,0" BorderBrush="Black">
                        <Border BorderThickness="0,0,1,1" BorderBrush="White">
                            <Button Width="25" Height="25" Content="\/"/>
                        </Border>
                    </Border>
                    <Border DockPanel.Dock="Bottom" BorderThickness="1,1,0,0" BorderBrush="Black">
                        <Border BorderThickness="0,0,1,1" BorderBrush="White">
                            <Button Width="25" Height="25" Content="/\"/>
                        </Border>
                    </Border>
                </DockPanel>
                <Border DockPanel.Dock="Bottom" BorderThickness="1,1,0,0" BorderBrush="Black" Margin="15,0,10,15">
                    <Border BorderThickness="0,0,1,1" BorderBrush="White">
                        <TextBlock Text="Triebfahrzeug auswählen, Bestätigen mit E, Abbrechen mit C" TextAlignment="Center"/>
                    </Border>
                </Border>
                <Border DockPanel.Dock="Bottom" BorderThickness="1,1,0,0" BorderBrush="Black" Margin="15,0,10,5" Height="40">
                    <Border BorderThickness="0,0,1,1" BorderBrush="White">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{Binding SelectedService.Info}" d:Text="57031 17.03.24 12:06:00, RE 57031 - München Hbf, BR143 DB"/>
                            <TextBlock Grid.Row="1" Text="{Binding SelectedService.ConfigName}" d:Text="[HBK] Hamburg-Lübeck"/>
                        </Grid>
                    </Border>
                </Border>
                <TextBlock DockPanel.Dock="Bottom" Text="Ausgewählter Fahrplan:" TextAlignment="Center" Margin="15,0,10,5"/>

                <Border DockPanel.Dock="Top" BorderThickness="1,1,0,0" BorderBrush="Black" Margin="15,0,10,5">
                    <Border BorderThickness="0,0,1,1" BorderBrush="White">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Text="Zugnummer und Fahrplan:" Margin="5,0,0,0"/>
                            <ListView ItemsSource="{Binding Services}" SelectedItem="{Binding SelectedService}" SelectedIndex="{Binding SelectedServiceIndex, Mode=OneWayToSource}"/>
                        </DockPanel>
                    </Border>
                </Border>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
